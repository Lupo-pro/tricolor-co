# Outreach System · LATRICOLOR.CO

Influencer outreach pipeline. Scrapes Colombian micro-influencers from Instagram + TikTok via Apify, filters + scores them, lets you validate via a swipe UI, then sends a 3-touch email sequence through Resend signed as Nay (brand owner).

> Full step-by-step setup + workflow lives in this file. Edit data in `/data/*.json`, never commit secrets — `/outreach/.env` and `/outreach/data/*.json` are gitignored.

The README is fleshed out in the final commit; this is a scaffold placeholder.
