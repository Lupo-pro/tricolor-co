# ============================================
# /content-machine/.env — copy this to /content-machine/.env
# Never commit the real .env (already in .gitignore).
# ============================================

# Claude (Anthropic)
ANTHROPIC_API_KEY=sk_xxxxxxxxxxxx

# PostEverywhere (publisher) — added in P2
POSTEVERYWHERE_API_KEY=pe_live_xxxxxxxxxxxx
POSTEVERYWHERE_INSTAGRAM_ACCOUNT_ID=xxxxxxxxxxxx

# Resend (reuse main project key)
RESEND_API_KEY=re_xxxxxxxxxxxx
NOTIFICATION_EMAIL=lupo24fit@gmail.com

# Brand
SITE_URL=https://latricolor.co

# Timezone — every schedule is computed in Colombia time
TIMEZONE=America/Bogota

# Local admin server
DASHBOARD_PORT=3002
